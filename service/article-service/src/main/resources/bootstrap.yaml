server:
  port: 8092
spring:
  application:
    name: article-service # 服务名称
  cloud:
    nacos:
      # nacos用户名
      username: nacos
      # nacos用户密码
      password: nacos
      # nacos服务器地址
      server-addr: localhost:8848
      # nacos配置中心相关
      config:
        file-extension: yaml
    